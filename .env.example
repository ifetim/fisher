# Copy to .env.local — never commit .env.local
PLAID_CLIENT_ID=your_client_id
PLAID_SECRET=your_sandbox_secret
PLAID_ENV=sandbox

# Optional — OpenAI for AI buttons (stubs work without it)
# Uses gpt-4o-mini to keep costs low. Set a $5 limit at platform.openai.com
OPENAI_API_KEY=your_key_here
